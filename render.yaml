services:
  - type: web
    name: student-task-reminder
    env: python
    plan: free
    region: eu-central
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
